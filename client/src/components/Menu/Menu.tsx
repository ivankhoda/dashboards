import React from "react";
import { MenuLink } from "../Link/Link";
import { StyledMenu } from "./StyledMenu";

export const Menu = () => {
  return (
    <StyledMenu>
      <MenuLink></MenuLink>
    </StyledMenu>
  );
};
